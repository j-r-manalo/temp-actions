name: Basic Reusable Workflow Test

on:
  workflow_dispatch:

jobs:
  test-reusable-workflow:
    permissions:
      contents: read     # Ensure the workflow can be read from a private repo
    uses: j-r-manalo/temp-reuesable/.github/workflows/simple-reusable.yml@main
